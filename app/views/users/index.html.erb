<% provide(:title, 'All users') %>
<h1><%= User.human_attribute_name(:all_users) %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_path(user) %>
    </li>
  <% end %>
</ul>

<%= paginate @items %>
<%= link_to t('views.common.back'), books_path %>
